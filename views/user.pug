extends main_layout

block content
  h1= user.username
  h4= user.email
  h4= user.mobile_number
  hr
    if user
      a.btn.btn-default(href='/users/edit/'+user._id) Edit
      a.btn.btn-danger.delete-user(href='#', data-id=user._id) Delete
